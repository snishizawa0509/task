<h1>投稿一覧</h1>
<%= page_entries_info @notes %>
<ul class="notes">
  <% @notes.each do |note| %>
    <li>
      <p>タイトル</p>
      <span class="content">
        <%= link_to note.title, note_path(note.id) %>
	<%= link_to "編集", note_edit_path(note.id), class: "btn edit-btn" %>     
	<%= link_to "削除", note_destroy_path(note.id), method:"delete", class: "btn edit-btn" %>    
      </span>
    </li>
  <% end %>
<%= paginate @notes %>

</ul>
